server:
    port: 8080
spring:
    session:
      store-type: none
    cloud:
        gateway:
            routes:
            - id: authenticator
              uri: http://localhost:8081/
              predicates:
              - Path=/authenticator/**
            - id: innosistemas
              uri: http://localhost:8082/
              predicates:
              - Path=/project/**
