spring.application.name=innosistemas
server.port=8082

# Database Configuration - PostgreSQL Neon (Cloud)
spring.datasource.url=jdbc:postgresql://ep-round-rain-ado66wet-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_NilC90RWPxTz
spring.datasource.driverClassName=org.postgresql.Driver

# Hace que Hibernate cree tablas desde las entidades
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jackson.serialization.indent_output=true

# Si agregas data.sql, que sí se ejecute
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# Actuator configuration with Prometheus
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=when-authorized
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true

# Kafka - DESHABILITADO para desarrollo local (sin Kafka local)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# JWT Secret
jwt.secret=your-secret-key-at-least-32-characters-long-for-security-please-change-this
