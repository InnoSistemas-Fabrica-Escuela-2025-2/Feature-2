spring.application.name=authenticator
jwt.secret=${JWT_SECRET:b7XfP9aQ2rL0sZ8wV3nC6mJ1yT4dE5kR}
jwt.expiration=900000
server.port=${PORT:8081}

# Database configuration - Using same as InnoSistemas for integration
spring.datasource.url=${AUTH_URL:jdbc:postgresql://ep-round-rain-ado66wet-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${AUTH_USERNAME:neondb_owner}
spring.datasource.password=${AUTH_PASSWORD:npg_NilC90RWPxTz}
spring.datasource.driverClassName=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jackson.serialization.indent_output=true

# SQL initialization
spring.sql.init.mode=never 
spring.jpa.defer-datasource-initialization=true

# Session configuration
spring.session.store-type=none

# Actuator configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true